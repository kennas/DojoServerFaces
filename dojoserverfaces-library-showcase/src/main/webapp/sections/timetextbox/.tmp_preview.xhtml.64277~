<?xml version="1.0" encoding="UTF-8" ?><!--
    	Copyright (c) 2011, The Dojo Foundation All Rights Reserved.
    	Available via Academic Free License >= 2.1 OR the modified BSD license.
    	see: http://dojotoolkit.org/license for details
 -->

<ui:composition
	xmlns="http://www.w3.org/1999/xhtml"
	xmlns:ui="http://java.sun.com/jsf/facelets"
	xmlns:f="http://java.sun.com/jsf/core"
	xmlns:h="http://java.sun.com/jsf/html"
	xmlns:c="http://java.sun.com/jsp/jstl/core"
	xmlns:dojo="http://www.dojoserverfaces.org/core"
>
	<h:form id="form">
		<h:panelGrid id="grid" columns="1">
			<h:panelGroup>
				<h:outputLabel for="timeTextBox" value="What's the time? "/>
				<dojo:timeTextBox id="timeTextBox" value="#{ timeTextBox.timeValue }"
					placeHolder="#{ timeTextBox.placeHolder }"
					locale="#{ timeTextBox.locale }"
					pattern="#{ timeTextBox.pattern }"
				
					disabled="#{ timeTextBox.disabled }"
					readOnly="#{ timeTextBox.readOnly }"
					required="#{ timeTextBox.required }"
					selectOnClick="#{ timeTextBox.selectOnClick }"
					
					am="#{ timeTextBox.am }"
					pm="#{ timeTextBox.pm }"
					clickableIncrement="#{ timeTextBox.clickableIncrement }"
					visibleIncrement="#{ timeTextBox.visibleIncrement }"
					visibleRange="#{ timeTextBox.visibleRange }"
					
					onBlur="#{ timeTextBox.onBlur }"
					onChange="#{ timeTextBox.onChange }"
					onClick="#{ timeTextBox.onClick }"
					onFocus="#{ timeTextBox.onFocus }"
					onSelect="#{ timeTextBox.onSelect }"
					
					style="width: 200px;"
				/>
			</h:panelGroup>
			
			<h:panelGroup>
				<dojo:button label="Submit">
					<f:ajax execute="@form" render=":result" />
				</dojo:button>
			</h:panelGroup>
		</h:panelGrid>
	</h:form>
	
	<hr class="separator" />
	<h:panelGrid columns="1" id="result">
		Time is : 
		<span style="font-weight: bold;">#{ timeTextBox.formattedTime }</span>
	</h:panelGrid>
</ui:composition>
